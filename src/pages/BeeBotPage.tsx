import KnowledgeWorkspacePage from "./KnowledgeWorkspacePage";

/**
 * BeeBot entry page — always renders the Obsidian-like knowledge workspace.
 * The AI Agent is embedded as a side panel inside KnowledgeWorkspacePage.
 */
export default function BeeBotPage() {
  return <KnowledgeWorkspacePage />;
}
